import 'package:flutter/material.dart';

/// Placeholder para la sección de alertas del sistema.
/// Se reemplazará con la implementación real cuando exista su spec.
class AlertsPage extends StatelessWidget {
  const AlertsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Alertas — próximamente'),
    );
  }
}
