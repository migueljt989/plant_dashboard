import 'package:flutter/material.dart';

/// Placeholder para la sección de histórico de lecturas.
/// Se reemplazará con la implementación real cuando exista su spec.
class ReadingsPage extends StatelessWidget {
  const ReadingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lecturas — próximamente'),
    );
  }
}
