import 'package:flutter/material.dart';

/// Placeholder para la sección de sensores.
/// Se reemplazará con la implementación real cuando exista su spec.
class SensorsPage extends StatelessWidget {
  const SensorsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sensores — próximamente'),
    );
  }
}
