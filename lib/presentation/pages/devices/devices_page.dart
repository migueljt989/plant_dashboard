import 'package:flutter/material.dart';

/// Placeholder para la sección de dispositivos IoT.
/// Se reemplazará con la implementación real cuando exista su spec.
class DevicesPage extends StatelessWidget {
  const DevicesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dispositivos — próximamente'),
    );
  }
}
